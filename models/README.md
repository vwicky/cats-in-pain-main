# Models directory (local)

Trained weights and pickles (e.g. YOLO `yolo11m.pt`, audio preclassifier `.pkl`) go here locally. **Contents** are git-ignored.

This file is tracked in git only as documentation.
