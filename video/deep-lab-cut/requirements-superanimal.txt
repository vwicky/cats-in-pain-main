# Shared deps for the ``dlc-superanimal`` conda env. TensorFlow is installed in
# ``install_superanimal_deps.sh`` (platform-specific): Apple arm64 uses
# ``tensorflow-macos`` + ``keras==2.12.0`` so ``keras.legacy_tf_layers`` exists
# (TensorFlow lazy-loads it from ``tensorflow.python.layers``). See DeepLabCut
# install docs: https://deeplabcut.github.io/DeepLabCut/docs/installation.html
# (``deeplabcut[tf]`` / TF 2.12 on Linux; M-chip notes there).
#
# NumPy 1.26.x stays <2 (required for TF 2.13 wheels) and satisfies current ``scipy`` wheels.
# The big ``pip install`` of DLC deps can still upgrade to NumPy 2.x; the install script
# re-pins NumPy after that step. ``typing_extensions>=4.10`` vs TF metadata: see install script.
setuptools>=69.5,<82
wheel
numpy>=1.24.3,<2.0
pandas>=2.0,<2.3
opencv-python>=4.8
pyyaml>=6.0
tqdm>=4.60
typing_extensions>=4.10
