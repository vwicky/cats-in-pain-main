Apply your system instructions to each item below.

Return **one JSON object** with a single key `"evaluations"`: an array of objects with
`video_id`, `decision` (`keep` or `discard`), `reason` (one sentence), and `confidence` (`high`, `medium`, or `low`).
Include **one evaluation per video** in the same order as listed.

Videos to evaluate:
