# Data directory (local)

Heavy datasets and CSVs live here on your machine. This folder’s **contents** are git-ignored so the repository stays small.

Copy files here manually when working locally (e.g. `data/video-data/unbalanced_train_segments.csv`).

This file is tracked in git only as documentation.
